; Standalone build for the OOK hardware probe (examples/ook_probe).
; Self-contained — does not pull in the PulseTape src/ tree.
;
;   cd examples/ook_probe
;   pio run -e esp32_lilygo1 -t upload
;   pio device monitor -e esp32_lilygo1 -b 115200
;
; (Or just open ook_probe.ino in Arduino IDE with esp32 core 3.x / 2.x, select
;  "TTGO LoRa32-OLED" or "ESP32 Dev Module", and Upload — no PlatformIO needed.)

[platformio]
src_dir = .

[env:esp32_lilygo1]
platform = espressif32@^6.6.0
board = ttgo-lora32-v1
framework = arduino
monitor_speed = 115200
build_src_filter = +<ook_probe.ino>
